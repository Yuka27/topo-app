
import { createTheme } from '@mui/material/styles';
import { lightGreen, purple, blueGrey } from '@mui/material/colors';

export const theme = createTheme({
    palette: {
        primary: lightGreen,
        secondary: purple,
        mode: 'dark'
    },
});
